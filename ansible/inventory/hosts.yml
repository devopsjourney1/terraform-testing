---
#ansible-inventory -i inventory/hosts.yml --graph

appservers:
  hosts:
    app1:
      ansible_user: ubuntu
#      app2:
#        ansible_user: ubuntu
#      app3:
#        ansible_user: ubuntu

control:
  children:
    master:
      hosts:
        control1:
          ansible_user: ubuntu
    agents:
      hosts:
        control2:
          ansible_user: ubuntu